<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="reply">
	<select id="selectQuestionReply" resultType="com.joomac.dto.ReplyDTO">
    SELECT rpno, rpcontent, rpdate, mno, qno
    FROM reply
    WHERE qno = #{qno}
  </select>

  <insert id="insertQuestionReply" parameterType="com.joomac.dto.ReplyDTO">
    INSERT INTO reply (rpno, rpcontent, rpdate, mno, qno)
    VALUES (reply_seq.NEXTVAL, #{rpcontent}, SYSDATE, #{mno}, #{qno})
  </insert>
	
	<select id="selectByBno" resultType="ReplyDTO">
    	SELECT * FROM reply WHERE bno = #{bno}
	</select>

	<insert id="insertBoardReply" parameterType="ReplyDTO">
	    INSERT INTO reply (
	        rpno, rpcontent, rpdate, mno, bno
	    ) VALUES (
	        reply_seq.NEXTVAL,
	        #{rpcontent},
	        SYSDATE,
	        #{mno},
	        #{bno}
	    )
	</insert>
	
</mapper>